<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manijee.edtechspark">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EdTechSpark">
        <activity
            android:name=".views.activities.SplashScreen"
            android:exported="true"
            android:theme="@style/style_splace"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.LoginPage"
            android:exported="true"
            android:theme="@style/style_splace"
            >

        </activity>
        <activity
            android:name=".views.activities.CreateUserActivity"
            android:exported="true"
            android:theme="@style/style_splace"
            >

        </activity>
    </application>

</manifest>